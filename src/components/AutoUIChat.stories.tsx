import { Meta } from '@storybook/react';
import AutoUIChat from './AutoUIChat';

export default {
  component: AutoUIChat,
} as Meta

export const Basic = () => {

  return (
    <AutoUIChat />
  )
}
