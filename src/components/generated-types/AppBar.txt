
export type AppBarRef = Readonly<{
  AppBar: AppBarRefProperties
}>

export type AppBarRefProperties = Readonly<{
  // The color of the component. It supports those theme colors that make sense for this component.
  color: 'default' | 'inherit' | 'primary' | 'secondary' | 'transparent'

  // The positioning type. The behavior of the different options is described in the MDN web docs:
  // https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Positioning
  position: 'fixed' | 'absolute' | 'sticky' | 'static' | 'relative'

  // The elevation of the app bar.
  elevation: number
}>
```

// The top App bar provides content and actions related to the current screen. It's used for branding, screen titles, navigation, and actions. It can transform into a contextual action bar or be used as a nav